<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:controls="clr-namespace:ProjectWord.Views.Controls"
             x:Class="ProjectWord.Views.Controls.CardContentView" x:DataType="controls:CardContentView">

    <Frame IsClippedToBounds="True" HasShadow="True" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Image x:Name="image" Grid.Row="0" Aspect="Fill">
                <Image.Source>
                    <UriImageSource x:Name="sourse" CacheValidity="14" CachingEnabled="False"/>
                </Image.Source>
            </Image>
            <StackLayout Grid.Row="1" Margin="10">
                <Label x:Name="title" FontSize="Subtitle" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                <Label x:Name="caption" FontSize="Caption" TextColor="{x:StaticResource Gray-500}"/>
            </StackLayout>
        </Grid>
    </Frame>
</StackLayout>