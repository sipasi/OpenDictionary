<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             
             xmlns:maui="clr-namespace:Microsoft.Maui.ApplicationModel;assembly=Microsoft.Maui.Essentials" 
             xmlns:viewmodels="clr-namespace:OpenDictionary.ViewModels" 
             xmlns:controls="clr-namespace:OpenDictionary.Views.Controls"
             xmlns:fonts="clr-namespace:OpenDictionary.Fonts.Icons"
             
             x:DataType="viewmodels:SettingsViewModel"
               
             Title="Settings"
             Padding="10"
             
             x:Class="OpenDictionary.Views.Pages.SettingsPage">

    <ContentPage.Resources>
        <Style TargetType="Label" BasedOn="{x:StaticResource DefaultLabel}">
            <Setter Property="FontSize" Value="Medium"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
        <Style TargetType="VerticalStackLayout">
            <Setter Property="Spacing" Value="14"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ButtonOutline}">

        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="BackgroundColor" 
                    Value="{AppThemeBinding Light={x:StaticResource White}, Dark={x:StaticResource Dark}}"/>
            <Setter Property="TextColor" Value="{AppThemeBinding Light={x:StaticResource Dark}, Dark={x:StaticResource White}}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout Spacing="10">
                <Frame>
                    <VerticalStackLayout>
                        <Label Text="Theme Color"/>

                        <VerticalStackLayout x:Name="radioGroup" Spacing="5"
                                 RadioButtonGroup.GroupName="theme-color" 
                                 RadioButtonGroup.SelectedValue="{Binding AppTheme.Current, Mode=TwoWay}">
                            <RadioButton Content="Day" Value="{x:Static maui:AppTheme.Light}"/>
                            <RadioButton Content="Night" Value="{x:Static maui:AppTheme.Dark}"/>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>
                <Frame>
                    <VerticalStackLayout>
                        <Label Text="Share As"/>

                        <Button Text="Json File"
                                Command="{Binding ImportExport.ShareCommand}" 
                                CommandParameter="{x:Static viewmodels:ShareOptions.JsonFile}"/>
                    </VerticalStackLayout>
                </Frame>
                <Frame>
                    <VerticalStackLayout>
                        <Label Text="Import From"/>

                        <Button Text="Json File" 
                                Command="{Binding ImportExport.ImportCommand}"/>
                    </VerticalStackLayout>
                </Frame>
                <Frame>
                    <VerticalStackLayout>
                        <Label Text="Dictionaries"/>

                        <VerticalStackLayout Spacing="0"> 
                            <Button Text="Add preinstalled"
                                Command="{Binding WordGroup.AddPreinstalledlCommand}"
                                Margin="0,0,0,10"/>
                            <Button Text="Delete All" 
                                Style="{x:StaticResource ButtonOutlineRed}"
                                Command="{Binding WordGroup.DeleteAllCommand}"/>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
